# Upstream Load Balancing Policy - POC

This policy allows to specify multiple upstreams and enables round robin load balancing on them. It accepts a list of hosts that will replace the Private Base URL.
